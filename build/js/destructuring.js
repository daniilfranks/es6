/*
let [js, ruby, golang] = ['js', 'ruby', 'golang'];
console.log(js, ruby, golang);

let scores = [0, 5, 10, 15, 20];
let [low, mid, high, ...rest] = scores;
console.log(low, mid, high, rest);

let user = {
  name: 'Den',
  age: 26
};

let { name, age } = user;
console.log(name, age);

let { f: f_name, l: l_name } = { f: 'bob', l: 'name' };
console.log(f_name, l_name);

*/
"use strict";